import fs as javascript from "fs"

fn callback => error content
  if error
    print "Une erreur est survenue : " + error
  print content

fs->readFile("export.ps" "UTF-8" callback)

number = "0"

print int : number

number = int : number

print str : number